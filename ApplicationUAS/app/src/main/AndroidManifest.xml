<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.applicationuas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ApplicationUAS">
        <activity android:name=".HasilSkor"></activity>
        <activity android:name=".Quiz" />
        <activity android:name=".Alphabet" />
        <activity android:name=".Kalimat" />
        <activity android:name=".lagu" />
        <activity android:name=".Mengeja" />
        <activity android:name=".MenuUtama" />
        <activity android:name=".Pendaftaran" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>